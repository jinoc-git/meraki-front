import React from 'react';

import * as S from './Loading.styled';

const Loading = () => {
  return (
    <S.LoadingContainer>
      <S.LoadingSpinner />
    </S.LoadingContainer>
  );
};

export default Loading;
